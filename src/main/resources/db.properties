# Database Configuration
# For local development - these are fallback values
# For production, set these as environment variables:
# DB_HOST, DB_PORT, DB_NAME, DB_USER, DB_PASSWORD

db.host=localhost
db.port=3306
db.name=eduhub
db.username=root
db.password=root
db.driver=com.mysql.cj.jdbc.Driver
