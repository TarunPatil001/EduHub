services:
  - type: web
    name: eduhub
    runtime: docker
    plan: free
    dockerfilePath: Dockerfile
    branch: dev
    buildCommand: ''
    startCommand: ''
    # Environment variables (DB_HOST, DB_PORT, DB_NAME, DB_USER, DB_PASSWORD, DB_SSL_MODE)
    # are configured manually in Render Dashboard for security